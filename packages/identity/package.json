{
  "name": "@platform/identity",
  "version": "0.1.0",
  "private": true,
  "description": "Production identity foundation — users, organizations, teams, RBAC (roles/permissions), profiles, database-backed sessions, and audit log.",
  "type": "module",
  "license": "UNLICENSED",
  "dependencies": {
    "@platform/core": "*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ]
}
