/**
 * @platform/ui — the product-agnostic UI component library and design
 * system. Re-exports both `ui-kit` (components) and `design-system`
 * (tokens); import the specific subpath (`@platform/ui/ui-kit`,
 * `@platform/ui/design-system`) if you only need one.
 */
export * from "./ui-kit/index";
export * from "./design-system/index";
