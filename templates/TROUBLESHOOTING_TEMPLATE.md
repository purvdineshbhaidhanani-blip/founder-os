# Troubleshooting — <Product Name>

Known issues and how to resolve them. Add an entry here whenever you (or a
user) hit something non-obvious — this file only earns its keep if it's
kept current.

## Local development

### `<Error message or symptom>`

**Cause:** <why it happens>
**Fix:** <exact steps>

---

## Integrations showing "not configured"

This is expected in Phase 1 — see `ONBOARDING.md` for the list of
integrations that are architecturally complete but disabled until real
credentials are supplied. This is not a bug.

## Database

### Migrations fail to apply

**Cause:**
**Fix:**

## Build / deploy

### Build fails in CI but works locally

**Cause:**
**Fix:**

---

<Add new sections/entries as real issues are found. Remove entries once
the underlying cause is fixed rather than leaving a stale workaround.>
